# 重构取舍说明

这不是对原文做同义词替换，而是把可复用原则重新装配成一套可执行总仓。原文件全部保留在外层工作区的 `原版/` 中。

## 保留了什么

| 原始能力 | 新位置 | 处理方式 |
|---|---|---|
| IOTO 的输入、产出、任务、成果分权 | 收件箱、三库、Harness、发布区 | 改成人类能直接判断的目录与状态 |
| AGENTS 权限矩阵、dry-run、索引和日志 | `AGENTS.md`、`policies/`、`index.md`、`log.md` | 去掉项目专属前缀，升级为默认拒绝 |
| 多源检索、QA、回溯、合成、终审 | Harness 三档路线 | 简单任务降级为单 Agent，复杂任务才用任务图 |
| Hermes 的记录、Skill、上下文、指标、复盘 | `60-Hermes运维/` | 运行事实与长期知识分开 |
| 工具库、Wiki、创意库三分 | `30`、根 LLMWiki 区、`50` | 增加新手入口和写回队列 |
| 世界书与角色卡映射 | `50-创意库/` | 源文件、导出物和运行时扩展分层 |

## 解决了哪些冲突

- `raw` 统一为不可改来源快照；不再一边说只读、一边原地注入元数据。
- 命名只保留 UTF-8、可读、稳定、无歧义等最小规则；项目专属前缀下放到项目局部规则。
- 新手 Frontmatter 保持五类字段；证据 ledger 与 trace 的机器字段留在 Harness 后台。
- 角色 Profile 缩成可执行契约；外貌、昵称、人格类型属于可选皮肤，不参与权限和正确性。
- 固定全员流水线改为 `direct / research / governed-write` 动态路由。
- 自动整理改为“只读扫描 → 计划 → 确认 → 应用 → 回读验证”，不把孤儿页自动视为垃圾。
- 查询结果不再直接修改正式 Wiki；先进入 `写回候选`。

## 没有迁入什么

- 武装 JK、Ena、Ele、Lena 等专属角色名、目录前缀和世界观设定。
- 未经证实的命令、硬编码 Linux 路径、固定数据源优先级和虚假的工作时间承诺。
- 要求模型输出隐藏推理的格式。
- `Ena_PULS_JK-2026.7.1_压缩版_追加装备栏限制.md` 中不适合通用或新手模板的未成年人性内容、惩戒与伤害概率设定；该文件整体不进入示例或模板。
- 第一版就上 RL、元学习、去中心化辩论或自动写正式知识页的设计。

## 版本边界

- LLMWiki 指 Hermes Agent 内置的 Markdown Wiki Skill；不默认混入其他同名 SQLite/MCP 项目。
- SillyTavern 产物按项目清单声明目标版本；本仓不把某一角色卡 JSON 版本永久写死为全局标准。
- Obsidian 社区插件是可选增强，不是新手首跑依赖。
