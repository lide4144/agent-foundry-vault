# 机器策略

这里存放 Harness 和检查脚本可读取的最小策略。人类规则以根 `AGENTS.md` 和 `SCHEMA.md` 为准；JSON 不能放宽它们。

- `permissions.json`：目录能力和人工门禁。
- `routes.json`：三档路由的触发条件。

权限路径重叠时采用“最具体路径优先”；文件也按具体规则在宽泛规则之前排列。任何实现若不支持该语义，必须拒绝加载，而不是退化为 first-match 放宽权限。

修改本目录属于治理变更，必须审阅差异并重跑全量检查。
