# 角色契约

角色契约不是人格设定，而是一份工作边界说明。本目录只保留八个必要部分：身份、目标、输入、输出、权限、禁区、升级条件、自检。

| 角色 | 主要职责 | 不负责 |
|---|---|---|
| `router` | 检查任务并选择运行档位 | 不生产业务答案 |
| `worker` | 在授权范围内执行读、查、转换 | 不写正式库 |
| `writer` | 把已核对结果变成可审批的变更提案 | 不绕过审批实施写入 |
| `reviewer` | 检查证据、约束、结果与写入门 | 不暗中重写产物 |

多个角色可以由同一个模型实例执行，但每一棒仍要按契约交接。

